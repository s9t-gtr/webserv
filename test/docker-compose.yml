service:
    nginx:
    build: ./requirements/nginx
    image: nginx
    container_name: nginx
    ports:
      - "443:443"
    networks:
      - default
    depends_on:
     - wordpress
    volumes:
      - wordpress:/var/www/html
    restart: always

